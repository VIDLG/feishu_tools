# Rust
/target
**/*.rs.bk

# Cargo lockfile (binary application, keep committed)
# (intentionally not ignored — fst is a CLI binary, not a library)

# pixi (env cache; keep pixi.lock committed for reproducibility)
.pixi/
.pixi-task-*/
.pixi-target-tests/

# Editor / IDE
.idea/
.vscode/
.zed/
*.iml

# OS junk
.DS_Store
Thumbs.db
nul

# Local secrets / config overrides
.env
.env.*
!.env.example

# Local runtime data
logs/
captures/
diagnostics/
dist/
